# Copy to .env at the repo root. Consumed by the containers in scripts/.

# Password for the local Seq container's "admin" user.
SEQ_ADMIN_PASSWORD=

# Password for the local Postgres container's "postgres" superuser. The climb
# role scripts/postgres.sh creates has its own password; this is the account
# that creates it.
POSTGRES_PASSWORD=
