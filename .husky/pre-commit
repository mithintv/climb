staged=$(git diff --cached --name-only --diff-filter=ACMR | wc -l | tr -d ' ')
pnpm exec lint-staged --quiet &&
	printf '\033[32m✔\033[0m biome — %s staged files · formatted, linted, imports sorted\n' "$staged"
